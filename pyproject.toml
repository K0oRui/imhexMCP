[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "imhex-mcp"
version = "0.4.0"
description = "ImHex MCP Server - AI assistant integration for ImHex hex editor"
requires-python = ">=3.10"


